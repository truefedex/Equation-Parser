package com.phlox.equation.parser;

import com.phlox.equation.operation.Operation;

public final class EquationParser {
	public static Operation parse(String equationValue) {
		for (int i = 0; i < equationValue.length(); i++) {
			//array_type array_element = equationValue[i];
			
		}
		return null;
	}
}
